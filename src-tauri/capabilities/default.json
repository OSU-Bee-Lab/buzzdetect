{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capability for the main window the model info window and the past runs window",
  "windows": ["main", "model-info", "history"],
  "permissions": [
    "core:default",
    "core:window:allow-close",
    "opener:default",
    "dialog:default"
  ]
}
