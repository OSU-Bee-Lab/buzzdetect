{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capability for the main window and the model info window",
  "windows": ["main", "model-info"],
  "permissions": [
    "core:default",
    "opener:default",
    "dialog:default"
  ]
}
