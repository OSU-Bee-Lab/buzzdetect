.DS_Store
node_modules
/build
/.svelte-kit
/package
.env
.env.*
!.env.example
vite.config.js.timestamp-*
vite.config.ts.timestamp-*

# Rust
/src-tauri/target

# engine/ is a git subtree of OSU-Bee-Lab/buzzdetect and brings its own
# .gitignore files (engine/.gitignore, engine/models/.gitignore,
# engine/embedders/.gitignore), which already cover __pycache__, .venv, and
# untracked model/embedder directories. Don't duplicate those rules here.
