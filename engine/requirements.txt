# A checkout's dependencies. The engine runs ONNX and nothing else, so this is
# requirements-onnx.txt plus what only a checkout uses (the legacy tkinter GUI
# in src/gui). TensorFlow is not here and is not needed: models are exported to
# a single ONNX graph ahead of time by buzzdetect-training's tools/export_onnx.py.
soxr
soundfile
pandas
numpy
matplotlib
av
onnxruntime
